/**
 * Babel metadata plugin for visual edits
 * This plugin adds metadata to components for visual editing
 */

module.exports = function babelMetadataPlugin() {
  // Return a no-op plugin for now
  // This can be extended later to add component metadata
  return {
    visitor: {
      // Empty visitor - can be extended later
    }
  };
};

