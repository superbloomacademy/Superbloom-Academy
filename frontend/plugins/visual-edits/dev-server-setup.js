/**
 * Visual edits dev server setup
 * This module configures the webpack dev server for visual editing features
 */

module.exports = function setupDevServer(devServerConfig) {
  // Return the dev server config as-is (no modifications needed for now)
  // This can be extended later to add visual editing capabilities
  return devServerConfig;
};

